{
  "name": "@ballclub/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ballclub/engine": "*",
    "animejs": "^3.2.2"
  },
  "devDependencies": {
    "@types/animejs": "^3.1.12",
    "typescript": "^5.6.0",
    "vite": "^8.2.1"
  }
}
