/**
 * Animation entry point. The demo loaded anime.js from a CDN with a
 * hand-rolled fallback shim; in production we bundle the real library.
 */
import anime from 'animejs';

export { anime };
export default anime;
